API service to publish cluster status to redis stream
    - read GPU status (memory and utilization)
    - running processes
    - connected users and SSH session length
    - system status (disk, memory usage, etc)

Monitoring and auto-shutdown service
    - watches instance for period of N minutes where no processes running and no users logged into SSH session
    - if user is logged in and no activity for N minutes (file watch) then ping as well
    - ping user via slack and warn of imminent shutdown in X minutes

Cloud backend status (implement Lambda backend for now)
    - cluster IP, basic metadata, etc.
    - Ability to terminate and restart an active instance

